# coding-portal

Local TypeScript workspace for algorithm / interview coding exercises.
Each problem lives in its own folder; a small HTTP portal (`npm run portal`) lists problems and runs tests.

## Project layout

```
<problem-id>/
  PROBLEM.md          # statement, examples, constraints, how to run tests
  HINTS.md            # spoilers — open only when stuck
  <Name>.ts           # stub — implement here (tests import this)
  .<Name>.ts          # hidden stub template (restored when finishing an attempt)
  <Name>.test.ts      # node:test suite (import `it` from ../test/it)
  solutions/          # local archived attempts (gitignored; spoilers; not used by tests)
progress.json         # local status (gitignored; pass | softpass | fail); portal updates this
portal/               # React+Vite UI (src/) + Node API (server.ts); practice timer in localStorage
test/it.ts            # shared `it` wrapper with default timeout
```

Portal discovers a problem when the folder contains `PROBLEM.md` and a `*.test.ts` file.
Do not put exercise folders under `portal/`, `test/`, or `node_modules/`.
`progress.json` and `**/solutions/` are gitignored — do not commit user practice state.

## Role when helping with exercises

This repo is for **practice**, not for shipping a solved dump.

- Prefer guiding: ask what was tried, point at complexity targets in `PROBLEM.md`, suggest approaches and edge cases.
- Do **not** paste a full working solution unless the user explicitly asks for one (e.g. "solve it", "give me the answer", "implement it").
- Treat `HINTS.md` and files under `<problem-id>/solutions/` as spoilers. Do not dump their contents unprompted; nudge toward opening them if the user is stuck.
- When implementing on request: edit the stub file (not archives under `solutions/`); keep the public signature; make tests pass; match complexity targets from `PROBLEM.md` when stated.
- After finishing an attempt via the portal: impl is archived under `<problem-id>/solutions/`, status is written to `progress.json`, and the stub is restored from `.<Name>.ts`.
- Prefer clear, interview-style TypeScript (readable over clever). Avoid unrelated refactors.

## Adding or editing a problem

When creating a new exercise, mirror an existing folder (e.g. `climbing-stairs/`):

1. `PROBLEM.md` — title (`# Name`), difficulty, topics, target complexity, statement, examples, constraints, file table, test command.
2. Stub `<Name>.ts` — exported signature only; body `throw new Error("Not implemented")`. No comments.
3. Hidden template `.<Name>.ts` — identical copy of the stub; used by the portal to restore the file after finishing an attempt.
4. `.test.ts` — `node:assert/strict` + `describe` from `node:test` + `it` from `../test/it`. Cover examples and important edge cases.
5. `HINTS.md` — progressive spoilers (idea → recurrence/structure → complexity note). Mark as spoilers at the top.
6. Optional `package.json` script: `"test:<id>": "npm run test:run -- <dir>/<file>.test.ts"`.

Do not change unrelated problems or the portal unless the task requires it.

## Tests & tooling

- Runner: `tsx --test` (`npm run test:run -- <path>` or a named `test:*` script).
- Always import `it` from `../test/it` (not raw `node:test`) so the default timeout applies.
- Portal: `npm run portal` (API on 3456 + Vite UI on 5173). `portal:build` then API can also serve `portal/dist`.

## Git commit messages

Write commit messages in English, imperative mood, Conventional Commits, subject under 72 chars, no trailing period on the subject.

Format: `<type>(<scope>): <summary>`

Focus the summary (and optional body) on **why** the change matters for the product or practice workflow — not on process filler. Never use empty justifications such as “for consistency”, “for streamlining”, “for clarity”, “for readability”, “cleanup”, or similar boilerplate; those are obvious and do not belong in the message.

Allowed types: `feat`, `fix`, `refactor`, `docs`, `test`, `chore`, `style`.

Scopes — pick from what changed:

- problem folder id (e.g. `climbing-stairs`, `edit-distance`)
- `portal` — portal server or UI
- `test` — shared test helpers
- `config` — `package.json`, `tsconfig.json`, tooling
- `deps` — dependency updates

Examples:

```
feat(climbing-stairs): add climbing stairs exercise stub and tests
fix(edit-distance): handle empty-string base cases
docs(lru-cache): clarify O(1) get/put target in PROBLEM.md
test(min-window): cover duplicate-character windows
chore(portal): stream test output without buffering
```
