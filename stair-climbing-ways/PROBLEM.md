# Stair Climbing Ways

**Difficulty:** Easy  
**Topics:** Counting  
**Target:** `O(n)` time, `O(1)` or `O(n)` space

## Problem

A flight has `n` steps. From a landing you may go up **1** step or **2** steps. Count how many sequences of strides reach the top.

Order matters: `1` then `2` is different from `2` then `1`.

## Examples

```
Input:  n = 4
Output: 5
Explanation: 1+1+1+1, 1+1+2, 1+2+1, 2+1+1, 2+2

Input:  n = 1
Output: 1
```

## Constraints

- `1 <= n <= 45`

## Files

| File | Role |
|------|------|
| `stairClimbingWays.ts` | Stub — implement here |
| `stairClimbingWays.test.ts` | Tests |
| `solutions/` | Archived attempts (spoilers) |
| `HINTS.md` | Spoilers — open only if stuck |

```bash
npm run test:stair-climbing-ways
```
